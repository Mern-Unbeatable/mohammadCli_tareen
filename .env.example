# API base URL — must include `/api/v1` (matches Postman `{{baseUrl}}`)
# Local server default port is 4000.
VITE_API_BASE_URL=http://localhost:4000/api/v1

# Production example:
# VITE_API_BASE_URL=https://mohamed430api.maktechgroup.tech/api/v1
